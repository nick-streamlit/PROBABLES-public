[project]
name = "probables"
version = "0.1.0"
description = "Fetch probable pitchers for the next 7 days"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "requests>=2.31.0",
    "pillow>=10.0.0"
]
